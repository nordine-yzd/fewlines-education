function tellFortune(job, city, name, age) {
  // Code the function here.
  console.log(`You will be ${job} in ${city}, and married to ${name} in ${age} years.`); 
}

// Do not remove last lines, it is for tests
module.exports = tellFortune;
