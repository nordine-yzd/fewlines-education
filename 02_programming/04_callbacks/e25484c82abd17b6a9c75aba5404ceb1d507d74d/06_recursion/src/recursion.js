const findPosition = (array,string,number) => {
  // Code here
};

const result = findPosition(["Fenn", "Florian", "Martin", "Louis", "Thomas"], "Martin", 0);

console.log(result);


// Leave line below for tests to work properly
module.exports = findPosition;
