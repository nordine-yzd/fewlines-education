const findPosition = (arrayOfString, string, number) => {
  // Code here
  if (typeof arrayOfString == "object" && typeof string === "string" && Number.isInteger(number) === true) {
    console.log();
  }
};

const result = ["Fenn", "Florian", "Martin", "Louis", "Thomas"];
console.log(typeof result);
console.log(result);

// Leave line below for tests to work properly
module.exports = findPosition;
