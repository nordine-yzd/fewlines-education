{% extends "layout.njk" %}


{% block content%}

<ul>
{% for element in platt %}

  <div align="left">
	    <h3>{{ element.name }}</h3>
  </div>

  <div align="center">
{% if element.logo.url %}
  <img src="{{element.logo.url}}" alt="{{element.name}}">
{% else %}
  <h1>Il n'y a pas de photos</h1>
  {# <img src="{{element.logo.url}}" alt="{{element.name}}"> #}
{% endif %}
  </div>

{% endfor %}
</ul>

{% endblock %}


{# <div align="center">Coucou je suis au mileu</div>
<p align="center">Je suis le texte à droite</p>
<p align="left">ils m'ont laisser tout seul à GAUCHE </p> #}
